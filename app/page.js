export const dynamic = "force-dynamic";

export default function HomePage() {
  return (
    <main style={{ padding: "40px", fontFamily: "system-ui, sans-serif" }}>
      <h1>weave-mcp</h1>
      <p>Legacy UI disabled. Use the v2 API route at <code>/api/v2/agent</code>.</p>
    </main>
  );
}
